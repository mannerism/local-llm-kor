{
  "name": "local-llm-guide",
  "version": "0.1.0",
  "description": "맥북에서 로컬 LLM 굴리기 — 한국어 입문 가이드",
  "type": "module",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "dev": "node scripts/sync.mjs --forward && vitepress dev docs",
    "build": "node scripts/sync.mjs --forward && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.7.0",
    "vitepress": "^1.6.4"
  }
}
